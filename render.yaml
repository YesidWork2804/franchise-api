services:
  - type: web
    name: franchise-api
    runtime: docker
    plan: free
    branch: master
    autoDeploy: true
    healthCheckPath: /actuator/health
    envVars:
      - key: DB_URL
        sync: false
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
